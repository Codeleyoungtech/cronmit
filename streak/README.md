# Cronmit Streak Log

Cronmit writes one dated markdown file per day and one append-only NDJSON audit log.

The workflow must never use `git commit --allow-empty`. Every commit should contain a real tracked file change.
